export type Resolution = {
  width: number
  height: number
}

export type MediaDevice = {
  videoId: string
  videoName: string
  audioId?: string
  audioName?: string
}

export type Mouse = {
  left: boolean
  right: boolean
  mid: boolean
}
